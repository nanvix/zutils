[package]
name = "hello-zlib"
version = "0.1.0"
nanvix-version = "0.12.410"

[builds]
[builds.matrix]
platforms = ["hyperlight", "microvm"]
modes = ["standalone", "single-process", "multi-process"]
memory = ["128mb"]

[dependencies]
zlib = "1.3.1"

[system-dependencies]
